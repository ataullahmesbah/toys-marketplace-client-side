import React from 'react';

const CatagoreyToyInfo = ({ info }) => {
    const { name, price, image, rating } = info;
    return (
        <div>

        </div>
    );
};

export default CatagoreyToyInfo;